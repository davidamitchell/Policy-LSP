{
  "servers": {
    "gov-lsp": {
      "command": "bash",
      "args": ["scripts/lsp-start.sh"],
      "env": {
        "GOV_LSP_POLICIES": "./policies"
      },
      "documentSelector": [{ "pattern": "**/*" }],
      "description": "GOV-LSP: governance language server — streams policy violations as LSP Diagnostics on every file open or change, no IDE required."
    }
  }
}
